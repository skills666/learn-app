{
  "name": "learn-app",
  "version": "1.0.0",
  "description": "面试题学习台 - 本地·离线·私有·艾宾浩斯闪卡记忆",
  "private": true,
  "scripts": {
    "sync:www": "node sync-www.js",
    "check": "node scripts/check-syntax.js && node scripts/check-globals.js",
    "cap:add:android": "cap add android",
    "cap:sync": "node sync-www.js && cap sync android",
    "cap:open": "cap open android",
    "build:android": "node sync-www.js && cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/android": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
